{
  "operation": {
    "operation-type": "create-collection",
    "collection": "geonames",
    "configset-path": "configsets/geonames",
    "num-shards": {{ num_shards | default(1) }},
    "replication-factor": {{ replication_factor | default(1) }}
  }
}
